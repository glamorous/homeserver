# Non-secret defaults, read automatically by Compose.
TZ="Europe/Brussels"
PUID=1000
PGID=1000

PIHOLE_PORT_HTTP=88
PIHOLE_PORT_HTTPS=444
# single: container on one network (default). all: on more than one.
PIHOLE_LISTENING_MODE=single
