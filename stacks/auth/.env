# Non-secret defaults, read automatically by Compose.
TZ="Europe/Brussels"

# The reverse proxy terminates TLS, so only the plain port is published and
# authentik's own HTTPS port stays inside. 9443 is deliberately avoided: it is
# already taken on a host that runs Portainer.
AUTHENTIK_PORT_HTTP=9100
