# Non-secret defaults, read automatically by Compose.
OPEN_WEBUI_PORT=3000
# The model server runs natively on the host, not in a container.
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Label on the sign-in button.
OPEN_WEBUI_OAUTH_NAME=authentik
